# Python
__pycache__/
*.pyc
.venv/
venv/

# Playwright
.playwright/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# scratch
*.log
tmp/

# anotações pessoais NUNCA são commitadas (repo público — ADR-008)
data/anotacoes.json

# planilhas brutas de ITBI da Prefeitura (~65 MB, atualizadas mensalmente) —
# nunca commitadas; só o JSON derivado data/transacoes.json entra no repo (ADR-011)
itbi/raw/
*.xlsx
